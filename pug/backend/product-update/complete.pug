extends ../../layout/_layout.pug

block title
    title 商品修正(完了)

block content
    h2 商品修正(完了)
    .complete-message(th:text="|商品名${p.name}を修正しました。|")
    a.botton(href="/admin/product"): button 商品検索へ戻る
    //- a.botton(href="/admin/product-add"): button 入力に戻る